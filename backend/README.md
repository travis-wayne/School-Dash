# backend

To install dependencies:

```bash
pnpm install
```

To run development:

```bash
pnpm dev
```

To run production:

```bash
pnpm start
```

To type-check:

```bash
pnpm build
```

Note: Requires Node.js v18+ and pnpm. Copy `.env.example` to `.env` before running.
